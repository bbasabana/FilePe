DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
NEXTAUTH_SECRET=change-me-to-a-long-random-string

# Agent empreintes local (Live20R) — ne change rien sur Vercel, c'est toujours localhost
NEXT_PUBLIC_FINGERPRINT_AGENT_URL=ws://127.0.0.1:8765
